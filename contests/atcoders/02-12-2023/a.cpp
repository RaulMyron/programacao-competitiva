#include<bits/stdc++.h>

using namespace std;

#define int long long

int main(){

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int n, k, t;

    cin >> n, k, t;

    vector<int> a;

    for (int i = 0; i < k; i++)
    {
        int aux;
        cin >> aux;
        a.push_back(aux);
    }
    
    

    return 0;
}